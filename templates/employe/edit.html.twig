{% extends 'base.html.twig' %}
{% block title %}Éditer un employé{% endblock %}

{% block body %}
    <h1>Éditer un employé</h1>

    <div class="card">
        <div class="card-body">
            {{ include('employe/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('app_employe_index') }}">Tous les employés</a>

    {{ include('employe/_delete_form.html.twig') }}
{% endblock %}
